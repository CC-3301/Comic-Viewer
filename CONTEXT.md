# Comic-Viewer

安卓漫画阅读器：从多种来源浏览并阅读漫画，支持条漫与单页两种阅读模式，兼容鼠标与触屏操作。

## Language

### 内容与来源

**来源（Source）**：
APP 获取漫画的渠道，共四种：本地存储、SMB、WebDAV、Komga。
_Avoid_: 书源、服务器

**连接（Connection）**：
用户配置的一个来源实例（地址、凭据等）。同一来源可建立多个连接。
_Avoid_: 账号、站点

**书（Book）**：
阅读器打开的最小阅读单位——一个 CBZ/ZIP 压缩包、一个图片文件夹，或 Komga 中的一本电子书。
_Avoid_: 文件、漫画、话

**系列（Series）**：
Komga 服务器按元数据聚合的一组书。仅 Komga 来源存在此概念。
_Avoid_: 文件夹

**发布日期（Release Date）**：
书级元数据中的日期，来自 CBZ 内 ComicInfo.xml 的 Year/Month/Day 字段。
_Avoid_: 创建时间、修改时间

**封面（Cover）**：
书或文件夹在浏览列表与书柜中显示的代表图：书取其第一页；多子文件夹的文件夹取第一个子文件夹的首页（逐级下取直到取到图）。
_Avoid_: 缩略图、海报

### 阅读

**条漫模式（Webtoon Mode）**：
垂直连续滚动、无翻页动作的阅读模式。
_Avoid_: 滚动模式、长条模式

**单页模式（Paged Mode）**：
一次显示一页、横向翻页的阅读模式，分左→右与右→左（RTL）两个方向。
_Avoid_: 翻页模式

**单页方向（Page Direction）**：
单页模式的横向推进方向——左→右（LTR）或右→左（RTL）；只决定横向排列与滑动推进方向，不改变触摸区域语义（左区恒为上一页）。
_Avoid_: 翻页方向、阅读方向

**阅读进度（Reading Progress）**：
某本书当前读到第几页的记录。本地持久保存；Komga 来源与服务器双向同步。
_Avoid_: 书签、历史记录

**触摸区域（Touch Zones）**：
屏幕纵向三等分的点击分区——左区上一页、中区呼出菜单、右区下一页；语义与阅读模式、单页方向无关（条漫/单页、LTR/RTL 下左区恒为上一页）；位于首页/末页时左右区跨到上一本/下一本，但需两段式确认（首点弹出底部确认条，点击条内橙色按钮才执行）；菜单底部按钮跳书不确认。
_Avoid_: 热区、点击区域样式

**双击放大（Double-tap Magnify）**：
以双击位置为中心局部放大的手势，放大倍率可在设置中调整；放大状态按页记忆、翻页不自动复位，仅再次双击恢复适屏；放大状态仅存在于本次阅读会话，退出阅读器后不保留。
_Avoid_: 放大镜

**双指缩放（Pinch Zoom）**：
双指开合缩放画面的手势，缩放幅度跟随手指动作。
_Avoid_: 捏合缩放

**列表进度条（List Progress Bar）**：
浏览列表中已读书目条目底部显示的阅读进度指示条；进行中为部分填充的绿色，读完后为满格红色。
_Avoid_: 进度显示

**始终从第一页打开（Open at First Page）**：
开启后打开任何书都定位到第一页的设置开关；打开瞬间阅读进度即被覆盖为第 1 页（进入马上退出也只算读了 1 页）。
_Avoid_: 总是开启第一个文件

**页面预览（Seekbar Preview）**：
拖动跳页滑动条时展示的目标页附近页面的缩略图预览。
_Avoid_: 缩略图模式

**上一本/下一本**：
阅读时跳到与本当前书相邻的书；相邻关系只由名称自然排序决定，与当前列表使用的排序方式无关。
_Avoid_: 上一话/下一话

**导航抽屉（Navigation Drawer）**：
从屏幕左缘滑出的面板（仅支持左侧），包含阅读器、书柜、设置三个入口。
_Avoid_: 侧滑菜单、汉堡菜单

**书柜（Bookshelf）**：
按连接分柜陈列该连接**根条目**（文件夹、书、系列）的界面；多连接的柜不混排，用户先选柜再进入柜内；柜内**书条目**显示进度条（进行中=部分填充绿，读完=满格红），文件夹与系列不显示进度条。
_Avoid_: 图书馆、收藏

**启动页面（Startup Page）**：
APP 启动时进入的界面，可在设置中选择，默认为上次阅读的位置。
_Avoid_: 首页行为、默认页

**上次阅读的位置（Last Read Position）**：
启动页面默认选项——上次退出时正在看书才直接打开该书并定位到上次页码；否则退化为上次停留的位置。
_Avoid_: 续读

**上次停留的位置（Last Browsing Location）**：
启动页面选项——恢复退出时所停留的浏览界面状态（目录层级与排序方式，不含滚动位置）。
_Avoid_: 会话恢复

**浏览历史（Browse History）**：
浏览层级间的后退/前进记录；后退侧键与系统返回手势触发后退，前进侧键触发前进；前进不回到阅读器。
_Avoid_: 阅读历史、历史记录

### 排序与状态

**名称自然排序（Logical Name Sort）**：
与 Windows 资源管理器一致的名称比较规则——特殊字符先于数字，数字先于字母；数字按数值比较（2 < 10）；字母不区分大小写；中文按拼音。
_Avoid_: 字典序、字母排序

**修改时间排序（Modified Time Sort）**：
按文件的最后修改时间（mtime）排序；所有来源支持。
_Avoid_: 时间排序、更新时间排序

**发布时间排序（Release Time Sort）**：
按书的发布日期排序；所有来源支持；书缺少发布日期元数据时（如图片文件夹）回退为修改时间排序。
_Avoid_: 发行时间排序
