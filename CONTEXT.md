# Comic-Viewer

安卓漫画阅读器：从多种来源浏览并阅读漫画，支持条漫与单页两种阅读模式，兼容鼠标与触屏操作。

## Language

### 内容与来源

**来源（Source）**：
APP 获取漫画的渠道，共四种：本地存储、SMB、WebDAV、Komga。
_Avoid_: 书源、服务器

**连接（Connection）**：
用户配置的一个来源实例（地址、凭据等）。同一来源可建立多个连接。
凭据（SMB/WebDAV 密码、Komga 的 API Key/密码）**加密落库**（票 #27）：表单与展示层拿到的始终是明文，加密只发生在存储层。
_Avoid_: 账号、站点

**书（Book）**：
阅读器打开的最小阅读单位——一个 CBZ/ZIP 压缩包、一个图片文件夹（**本层只有图片**的目录），或 Komga 中的一本电子书；压缩包内部不按目录分卷，包内图片全深度收集（票 #97）。
_Avoid_: 文件、漫画、话

**容器（Container）**：
浏览页里点开继续下钻的目录——本层有子文件夹或压缩包（即本层不只有图片）的目录。容器**不递归合并**：列出的是它自己这一层的条目，子文件夹与压缩包各是一条独立条目（点一个读一个、各自是一本书），本层图片各自也是条目（点一张 = 从该张连读本层其余图片）。
_Avoid_: 文件夹（作为阅读单位时说）、目录

**系列（Series）**：
Komga 服务器按元数据聚合的一组书。仅 Komga 来源存在此概念。
_Avoid_: 文件夹

**发布日期（Release Date）**：
书级元数据中的日期，来自 CBZ 内 ComicInfo.xml 的 Year/Month/Day 字段。
_Avoid_: 创建时间、修改时间

**封面（Cover）**：
书或文件夹在浏览页（列表档与网格档）中显示的代表图：书取其第一页；本层有图的文件夹（票 #97 起这类目录是容器）取其本层首图；多子文件夹的文件夹取第一个子文件夹的首页（逐级下取直到取到图）。
_Avoid_: 缩略图、海报

### 阅读

**条漫模式（Webtoon Mode）**：
垂直连续滚动、可按页跳转（跳到下一页/上一页页首）的阅读模式。
_Avoid_: 滚动模式、长条模式

**单页模式（Paged Mode）**：
一次显示一页、横向翻页的阅读模式，分左→右与右→左（RTL）两个方向。
_Avoid_: 翻页模式

**单页方向（Page Direction）**：
单页模式的横向推进方向——左→右（LTR）或右→左（RTL）；只决定横向排列与滑动推进方向，不改变触摸区域语义（左区恒为上一页）。
_Avoid_: 翻页方向、阅读方向

**阅读进度（Reading Progress）**：
某本书当前读到第几页的记录。本地持久保存；Komga 来源与服务器双向同步。
_Avoid_: 书签、历史记录

**触摸区域（Touch Zones）**：
屏幕纵向三等分的点击分区——左区上一页、中区呼出菜单、右区下一页；语义与阅读模式、单页方向无关（条漫/单页、LTR/RTL 下左区恒为上一页）；位于首页/末页时左右区跨到上一本/下一本，但需两段式确认（首点弹出底部确认条，点击条内橙色按钮才执行）；菜单底部按钮跳书不确认。
_Avoid_: 热区、点击区域样式

**双击放大（Double-tap Magnify）**：
以双击位置为中心局部放大的手势，放大倍率可在设置中调整；放大状态按页记忆、翻页不自动复位，仅再次双击恢复适屏；放大状态仅存在于本次阅读会话，退出阅读器后不保留。
_Avoid_: 放大镜

**双指缩放（Pinch Zoom）**：
双指开合缩放画面的手势，缩放幅度跟随手指动作。
_Avoid_: 捏合缩放

**列表进度条（List Progress Bar）**：
浏览页条目上的阅读进度指示条（两档位置不同：列表档在**名称正下方、与名称左缘对齐**（名称那一列内，**条右端到名称列右缘（即行内容右缘），不做内缩**）；网格档**压在封面下缘**、不占布局；条样式两档一致：轨道**跟主题走**（`onSurface` 的 30% 不透明度，有意可透出底图）、方头端帽、高 6dp；**网格档**的条下另铺一层黑 45% 暗底（与条同宽同高、叠在封面下缘、不占布局；列表档没有这层；未读时不画）；只对**书条目**显示，文件夹与系列没有进度，不显示）；进行中为部分填充的绿色，读完后为满格红色。
_Avoid_: 进度显示

**始终从第一页打开（Open at First Page）**：
开启后打开任何书都定位到第一页的设置开关；打开瞬间阅读进度即被覆盖为第 1 页（进入马上退出也只算读了 1 页）。
_Avoid_: 总是开启第一个文件

**页面预览（Seekbar Preview）**：
阅读菜单打开时即展示当前页 ±2 的页面缩略图（带页码、当前页高亮）；拖动跳页滑动条时跟随目标页，松手后回到当前页。
_Avoid_: 缩略图模式

**上一本/下一本**：
阅读时跳到与本当前书相邻的书；相邻关系只由名称自然排序决定，与当前列表使用的排序方式及方向都无关。
_Avoid_: 上一话/下一话

**导航抽屉（Navigation Drawer）**：
从屏幕左缘滑出的面板（仅支持左侧），包含首页、阅读器、书柜、设置四个入口；浏览历史的后退/前进不在抽屉里，只由系统返回手势与鼠标侧键触发。
_Avoid_: 侧滑菜单、汉堡菜单

**书柜（Bookshelf）**：
按连接分柜的**入口列表**（柜名取自连接配置，离线连接也列）；点某连接即进入该连接的**浏览页根层**——与首页点连接是同一路由、同一屏、同一排版，不存在单独的「柜内」屏。
_Avoid_: 图书馆、收藏

**首页（Home）**：
列出四种来源（本地、SMB、WebDAV、Komga）入口的界面；只放来源入口，不放设置（设置只由导航抽屉到达）。
_Avoid_: 主页、起始页

**启动页面（Startup Page）**：
APP 启动时进入的界面，可在设置中选择，默认为上次阅读的位置。
_Avoid_: 首页行为、默认页

**上次阅读的位置（Last Read Position）**：
启动页面默认选项——上次退出时正在看书才直接打开该书并定位到上次页码；否则退化为上次停留的位置。
_Avoid_: 续读

**上次停留的位置（Last Browsing Location）**：
启动页面选项——恢复退出时所停留的浏览界面的**目录层级**（排序方式与方向属全局设置、不在恢复之列；不含滚动位置）。
_Avoid_: 会话恢复

**浏览历史（Browse History）**：
浏览层级间的后退/前进记录；后退侧键与系统返回手势触发后退，前进侧键触发前进；前进不回到阅读器。
_Avoid_: 阅读历史、历史记录

### 排序与状态

**排序设置（Sort Setting）**：
全 app 一份的排序方式（名称 / 修改时间 / 发布时间）与方向（正向 / 反向）设置；浏览页的条目列表（列表档与网格档）共用，跨目录层级、跨连接、跨重启保持。正向即三个比较器术语各自描述的规则本身（名称 A→Z、时间新→旧）；点当前排序类别再点一次即反向。
_Avoid_: 排序偏好、每页排序

**视图档位（View Mode）**：
全 app 一份的条目形态设置——列表 / 网格 2 列 / 网格 3 列 / 网格 4 列（默认网格 2 列，非法或缺失值也回落网格 2 列）；浏览页的条目列表按它渲染（列表档 = 行，网格档 = 按固定列数的格子），跨目录层级、跨连接、跨重启保持。
_Avoid_: 布局模式、显示方式、列数开关

**下拉更新（Pull to Refresh）**：
浏览页顶部按住向下拖动越过阈值即**失效当前层列表缓存并重新枚举**（含可见行重取封面）；只认拖拽（手指或鼠标左键），滚轮滚动一律不触发。
_Avoid_: 下拉刷新、上拉加载

**条目名称断行（Entry Name Wrapping）**：
条目名称按「能塞就塞」断行（最多两行、不省略号）：在 `]`、`-` 这类闭合/连接标点之后与数字字母交界处补零宽空格作为额外断点（票 #92 起：“长度 ≥ 4 的 ASCII 字母/数字串”在串内每 2 个字符也补一个，因此可就地断开、把行尾填满，不引入连字符；3 字符以下与其它文字保持原子），并给名称文本指定贪心断行配置，避免局部不可断单元把第一行提前截断而留下大段空白。
_Avoid_: 自动换行、文本截断

**名称自然排序（Logical Name Sort）**：
与 Windows 资源管理器一致的名称比较规则——特殊字符先于数字，数字先于字母，假名先于汉字；数字按数值比较（2 < 10）；字母不区分大小写；假名按五十音序（平假名与片假名同权）；汉字按拼音。
_Avoid_: 字典序、字母排序

**修改时间排序（Modified Time Sort）**：
按文件的最后修改时间（mtime）排序；所有来源支持。
_Avoid_: 时间排序、更新时间排序

**发布时间排序（Release Time Sort）**：
按书的发布日期排序；所有来源支持；书缺少发布日期元数据时（如图片文件夹）回退为修改时间排序。
_Avoid_: 发行时间排序
