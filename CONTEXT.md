# Comic-Viewer

安卓漫画阅读器：从多种来源浏览并阅读漫画，支持条漫与单页两种阅读模式，兼容鼠标与触屏操作。

## Language

本文件是**词表**：只放「术语 = 定义」。行为规格在 `docs/SPEC.md`，实现细节看代码与 KDoc，
逐条溯源看 git 历史与各票 issue。

### 内容与来源

**来源（Source）**：
APP 获取漫画的渠道，共四种：本地存储、SMB、WebDAV、Komga。
_Avoid_: 书源、服务器

**连接（Connection）**：
用户配置的一个来源实例（地址、凭据等）；同一来源可建立多个连接。

- 凭据（SMB/WebDAV 密码、Komga 的 API Key/密码）**加密落库**（票 #27）。
  - 表单与展示层拿到的始终是明文，加密只发生在存储层。
  - Komga 表单自票 #76 起**只提供邮箱 + 密码**（Basic 认证）；
    API Key 仅存量连接保留，读取路径不变。
- **浏览路径（browsePath）**（票 #78）：决定进连接后从哪一层开始。
  - `/` = 根层四个入口（收藏 / 系列 / 书籍 / 阅读过），默认就是它。
  - 表单里**只能点选**（键盘输入无效）；非法或缺失值一律回落 `/`。
  - 与 baseUrl 里的 **URL 子路径**不是一回事（后者是默认连接名里那段路径）。
  - 落库的段名是**稳定 token**（`/collections`、`/series`、`/books`、`/read`），
    不随界面文案变；解析兼容票 #78 首轮落过的中文段（`/收藏` 这类）。

_Avoid_: 账号、站点

**连接名（Connection Name）**：
连接的展示名，落 `connections.displayName` 列。

- 书柜柜名、浏览页根层标题、连接列表显示的是**列**；错误提示读来源配置的 `displayName`。
  配置由连接行构造时把列名作为**运行期载体**带上（票 #72 r2），两者因此恒等。
- 默认名 = `主机[:端口]/路径` 形态、**不带 scheme**：
  - SMB：`主机[:端口]/共享名/子目录`
  - WebDAV：`主机[:端口]/DAV根/起始目录`
  - Komga：`主机[:端口]/路径`
  - 本地：所选文件夹名
- **端口只在用户显式配置过时才显示**（SMB 的 445 同此口径：写了 `:445` 就显示）。
- 用户可在连接表单的「名称（可空）」里覆盖（本地来源在根列表里「重命名」）。
- **留空即回落默认名**；名称去首尾空白、超长截断（40 字）；都拿不到时用兜底名「未知连接」。
- 存量连接不批量重算，编辑保存后才切到新形态（与 `#38` 一致）。

_Avoid_: 别名

**书（Book）**：
阅读器打开的最小阅读单位——一个 CBZ/ZIP 压缩包、一个图片文件夹（**本层只有图片**的目录），
或 Komga 中的一本电子书；压缩包内部不按目录分卷，包内图片全深度收集（票 #97）。
_Avoid_: 文件、漫画、话

**容器（Container）**：
浏览页里点开继续下钻的目录——本层有子文件夹或压缩包（即本层不只有图片）的目录。

- 容器**不递归合并**：列出的是它自己这一层的条目。
- 子文件夹与压缩包各是一条独立条目（点一个读一个、各自是一本书）。
- 本层图片各自也是条目（点一张 = 从该张连读本层其余图片）。

_Avoid_: 文件夹（作为阅读单位时说）、目录

**系列（Series）**：
Komga 服务器按元数据聚合的一组书。仅 Komga 来源存在此概念。
_Avoid_: 文件夹

**收藏（Collection）**：
Komga 服务器上由用户组织的一组系列（票 #78）。

- 仅 Komga 来源存在此概念，是根层四入口之一。
- 点开它看到的内容**按服务端返回什么就渲染什么**：
  Komga 原生结构里收藏组织系列，阅读列表才组织书，本 app 不做阅读列表。
- 带 `media`/`seriesId` 的条目渲染为书行、其余为系列行。

_Avoid_: 书单、分组

**阅读过（Read）**：
浏览页根层四个入口之一（票 #78）：**有阅读记录的书**。

- 在读与已读完都算（Komga 的 `readStatus ∈ {IN_PROGRESS, READ}`），不含从未打开过的书。
- **固定按最近阅读倒序**（服务端 `readProgress.lastModified,desc`）——
  这是有意的例外：不跟随全局排序菜单的类别档（故事 14）。

_Avoid_: 阅读历史、已读列表

**发布日期（Release Date）**：
书级元数据中的日期，来自 CBZ 内 ComicInfo.xml 的 Year/Month/Day 字段。
_Avoid_: 创建时间、修改时间

**封面（Cover）**：
书或文件夹在浏览页（列表档与网格档）中显示的代表图。

- 书取其第一页。
- 本层有图的文件夹（票 #97 起这类目录是容器）取其本层首图。
- 多子文件夹的文件夹逐级下取（票 #102 起**每一层都是与单层目录同一套优先级**：
  本层图 → 本层首个压缩包的首帧 → 再下探子目录），直到取到图。
- Komga 的**容器行**（类别行、收藏行等取不到缩略图的容器）按名称序取**第一个子项的封面**，
  子项也没有则留空；该封面**不随全局排序变化**。

_Avoid_: 缩略图、海报

**列表快照（Listing Snapshot）**：
浏览页某容器「这一层有什么」的枚举结果——条目清单（名称 / 是否书 / 封面指向）
加上**该目录自身的 mtime**。

- 会话内按容器一份。
- 票 #74 起还按「连接 id + 容器 id」落盘到 APP 私有缓存目录
  （带写入时间与格式版本号，不落盘封面字节）。
- 因此退出 APP 再进入同一目录仍是 0 次列目录、0 次探测。
- **从阅读器返回浏览页时**立即可见；冷启动首帧仍可能短暂显示「加载中…」，
  但内容来自落盘快照、0 次列目录、0 次探测。
- 目录 mtime 变化、下拉更新、连接被编辑或删除都会让它失效。

_Avoid_: 列表缓存、目录缓存

### 阅读

**条漫模式（Webtoon Mode）**：
垂直连续滚动、可按页跳转（跳到下一页/上一页页首）的阅读模式。
_Avoid_: 滚动模式、长条模式

**单页模式（Paged Mode）**：
一次显示一页、横向翻页的阅读模式，分左→右与右→左（RTL）两个方向。
_Avoid_: 翻页模式

**单页方向（Page Direction）**：
单页模式的横向推进方向——左→右（LTR）或右→左（RTL）。

- 只决定横向排列与滑动推进方向。
- **不改变触摸区域语义**（左区恒为上一页）。

_Avoid_: 翻页方向、阅读方向

**阅读进度（Reading Progress）**：
某本书当前读到第几页的记录。本地持久保存；Komga 来源与服务器双向同步。
_Avoid_: 书签、历史记录

**触摸区域（Touch Zones）**：
屏幕纵向三等分的点击分区——左区上一页、中区呼出菜单、右区下一页。

- 语义与阅读模式、单页方向无关（条漫/单页、LTR/RTL 下左区恒为上一页）。
- 位于首页/末页时左右区跨到上一本/下一本，但需**两段式确认**：
  首点弹出底部确认条，点击条内橙色按钮才执行。
- 菜单底部按钮跳书不确认。

_Avoid_: 热区、点击区域样式

**双击放大（Double-tap Magnify）**：
以双击位置为中心局部放大的手势，放大倍率可在设置中调整。

- 放大状态按页记忆、翻页不自动复位，仅再次双击恢复适屏。
- 放大与恢复适屏都是约 240ms 的缓入缓出过渡（票 #59）：
  缩放后内容 ≥ 视口的那条轴上双击点整段过渡不动；
  页窄于视口的轴（单页模式横屏几乎必中）偏移由既有钳制公式决定。
- 双指缩放与放大后单指平移仍逐帧跟手。
- 放大状态仅存在于本次阅读会话，退出阅读器后不保留。

_Avoid_: 放大镜

**双指缩放（Pinch Zoom）**：
双指开合缩放画面的手势，缩放幅度跟随手指动作。
_Avoid_: 捏合缩放

**阅读器沉浸（Reader Immersion）**：
进入阅读器时隐藏状态栏与导航栏（手势导航下底部手势条由系统一并隐藏），离开即恢复。

- 由**当前路由**驱动——路由在场即隐藏、离开即恢复，与阅读页的组合是否存活无关。
  （票 #61：换书会换掉阅读页的 entry，按组合存活期开关会被旧 entry 的「恢复」盖回，
  见 `ui/ReaderSystemBars.kt`。）
- 沉浸态下边缘上拨只**临时**唤出系统栏、不产生导航行为（不回主页/不返回），
  几秒后自动再隐藏（`BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`）。
- 贴底浮层（阅读菜单面板、跨书确认条）的底部因此分两支：
  - 系统栏占位（可见）时用真实栏高。
    （边缘上拨瞬态唤出的那几秒是否也算占位属平台行为、**真机观察项**。）
  - 栏隐藏时用「挖孔底 ∪ 24dp 最小留白」（判据 `core/view/ReaderOverlayLayout.kt`）。
- **例外**：阅读菜单面板在**手机竖屏档**不消费这份底部 inset——
  面板底边贴屏幕下缘、底部内边距做成「滑条行半高 + 行距」使底部行上下两段各 36dp 成立
  （票 #105 裁决 C）；代价是底行连同其 48dp 命中带的下缘落进该区。其余视口档照旧消费。
- **真机验收未做**。

_Avoid_: 全屏模式、隐藏状态栏

**列表进度条（List Progress Bar）**：
浏览页条目上的阅读进度指示条；只对**书条目**显示，文件夹与系列没有进度、不显示。

- 位置两档不同：
  - 列表档：在**名称正下方、与名称左缘对齐**（名称那一列内）；
    **条右端到名称列右缘（即行内容右缘），不做内缩**。
  - 网格档：**压在封面下缘**、不占布局。
- 条样式两档一致：轨道**跟主题走**（`onSurface` 的 30% 不透明度，有意可透出底图）、
  方头端帽、高 6dp。
- **网格档**的条下另铺一层黑 45% 暗底（与条同宽同高、叠在封面下缘、不占布局；
  列表档没有这层；未读时不画）。
- 进行中为部分填充的绿色，读完后为满格红色。

_Avoid_: 进度显示

**始终从第一页打开（Open at First Page）**：
开启后打开任何书都定位到第一页的设置开关。

- **进度与「上次阅读位置」在真正切进阅读页那一刻写入**（票 #110）：
  浏览页点击后的前置准备阶段只开书、不写；点开又取消 / 被后一次点击顶替都不留副作用。
- 进入马上退出也只算读了 1 页。
- **例外**：前置还没到货就退出时不写——落地排在阅读页侧那次可取消的等待之后（票 #122）。

_Avoid_: 总是开启第一个文件

**页面预览（Seekbar Preview）**：
阅读菜单打开时即展示**全书页的横向预览条**（滑动式、每格带页码、当前页高亮）。

- 单格**宽度按该页真实比例**。
- **高度 = 预览条高 − 格内页码行**；预览条高度按视口档位取定、**不随标题行数变化**，
  手机竖屏保底 **201dp**（面板 40% = 320dp、滑条行 **28dp**、底部行上下间距各 36dp；
  票 #105 r13/r14）。
- **当前页缩略图始终居中**——跳页/滚动后落在预览区正中，首页与末页除外
  （到头只能贴边，票 #105 r13）。
- **格内页码在缩略图正下方居中**；注意它与「底部行的面板页码」是两回事，
  后者在底部行**中列**（票 #105 r9）。
- 三档字号基准都是面板内宽：
  - 标题 = 内宽 × 0.05 夹 18–24sp
  - 页码 = 内宽 × 0.05 夹 16–24sp
  - 格内页码 = 内宽 × 0.035 夹 12–16sp
- 拖动跳页滑动条时跟随目标页，松手后回到当前页。
- **单击滑动条轨道与拖动等价**（票 #63）：预览以该位置对应页为中心、随即跳到该页，菜单不关。
- **点击缩略图**直接跳到该页、**菜单保持打开**
  （票 #105 起：与滑动条跳页同一条落地路径，不再关菜单）。

_Avoid_: 缩略图模式

**上一本/下一本**：
阅读时跳到与本当前书相邻的书；相邻关系只由名称自然排序决定，
与当前列表使用的排序方式及方向都无关。
_Avoid_: 上一话/下一话

**导航抽屉（Navigation Drawer）**：
从屏幕左缘滑出的面板（仅支持左侧），包含首页、阅读器、书柜、设置四个入口。

- 浏览历史的后退/前进不在抽屉里，只由系统返回手势与鼠标侧键触发。

_Avoid_: 侧滑菜单、汉堡菜单

**书柜（Bookshelf）**：
按连接分柜的**入口列表**（柜名取自连接配置，离线连接也列）。

- 点某连接即进入该连接的**浏览页根层**——与首页点连接是同一路由、同一屏、同一排版，
  不存在单独的「柜内」屏。

_Avoid_: 图书馆、收藏

**首页（Home）**：
列出四种来源（本地、SMB、WebDAV、Komga）入口的界面。

- 只放来源入口，不放设置（设置只由导航抽屉到达）。

_Avoid_: 主页、起始页

**启动页面（Startup Page）**：
APP 启动时进入的界面，可在设置中选择，默认为上次阅读的位置。
_Avoid_: 首页行为、默认页

**上次阅读的位置（Last Read Position）**：
启动页面默认选项——上次退出时正在看书才直接打开该书并定位到上次页码；
否则退化为上次停留的位置。
_Avoid_: 续读

**上次停留的位置（Last Browsing Location）**：
启动页面选项——恢复退出时所停留的浏览界面的**目录层级**。

- 排序方式与方向属全局设置、**不在恢复之列**。
- **不含滚动位置**。

_Avoid_: 会话恢复

**浏览历史（Browse History）**：
浏览层级间的后退/前进记录。

- 后退侧键与系统返回手势触发后退，前进侧键触发前进；**前进不回到阅读器**。
- 会话结束（Activity finish，即退出 APP）时清空，与回退栈一并作废。
- 旋转这类非 finish 的 Activity 重建保留。

_Avoid_: 阅读历史、历史记录

### 排序与状态

**排序设置（Sort Setting）**：
全 app 一份的排序方式（名称 / 修改时间 / 发布时间）与方向（正向 / 反向）设置。

- 浏览页的条目列表（列表档与网格档）共用，跨目录层级、跨连接、跨重启保持。
- 正向即三个比较器术语各自描述的规则本身（名称升序、时间新→旧）。
- 排序菜单给的是 6 个显式选项（类别 × 方向）：名称论升序/降序、时间两类论新→旧/旧→新。
- 点一项即类别与方向同时生效，当前生效项打勾、按钮文字写当前类别与方向。

_Avoid_: 排序偏好、每页排序

**视图档位（View Mode）**：
全 app 一份的条目形态设置——列表 / 网格 2 列 / 网格 3 列 / 网格 4 列。

- 默认网格 2 列，非法或缺失值也回落网格 2 列。
- 浏览页的条目列表按它渲染（列表档 = 行，网格档 = 按固定列数的格子）。
- 跨目录层级、跨连接、跨重启保持。

_Avoid_: 布局模式、显示方式、列数开关

**下拉更新（Pull to Refresh）**：
浏览页顶部按住向下拖动越过阈值即**失效当前层列表快照并重新枚举**（含可见行重取封面）。
只认拖拽（手指或鼠标左键），滚轮滚动一律不触发。
_Avoid_: 下拉刷新、上拉加载

**鼠标拖动滚动（Mouse Drag Scroll）**：
浏览页（列表档与网格档）与条漫阅读器里**按住鼠标左键上下拖动即滚动**——
幅度跟手、松手有惯性（票 #69）。

- 只认鼠标左键，越过触摸斜率才算拖动，因此左键单击仍是点击
  （阅读器里仍按触摸区域执行）。
- 与下拉更新分层：列表停在顶部时向下拖动整段归下拉更新、本手势让位
  （不出现「又刷新又滚动」）。
- 单页模式不加（仍由滚轮/音量键翻页）。

_Avoid_: 鼠标滑动、拖拽滚动

**快速定位滑条（Quick Scroll Bar）**：
浏览页（列表档与网格档）右缘一条**细滑条**，用于 1000+ 条目时快速定位（票 #60）。

- 几何：宽 6dp、**本体居中于横向空档**——离屏缘 = （空档 − 本体宽）/ 2；
  空档 = 内容右留白 + 系统右缘 inset。
  - 无 inset 时空档 = 20dp ⇒ 本体右缘 7dp、抓取带 13dp。
  - 空档被撑宽时本体离屏缘随之变大、仍落在空档正中，抓取带随空档变宽。
  - 2026-09-23 维护者决定**恢复** r4 的「居中于空档」口径；
    批次 9 r2 的「贴屏幕侧固定（中心距屏缘 10dp）」已作废。
  - 右侧留白 12→20dp，封面相应变窄约 8dp。
- 半透明覆盖在内容之上。
- **出现/隐藏**：任意滚动或拖动时立即淡入（120ms）；静止约 1.2 秒后**只淡出一次**（200ms）。
  **按住或拖动期间不计时、也不重排计时**。
- **长度与位置**反映「当前视口 / 整份列表」的比例。
  - **按需加载的层里**（票 #119）分母是**已加载条数**（+ 截断提示行与尾部触发件行）——
    滑条表示的是已加载范围内的位置，更远的条目要先滚到底续页。
  - **位置与长度都取连续值**：进度 = （首个可见**行**索引 + 行内已滚过比例）÷ 总行数。
  - **按行不按条目**：网格档一档多格，按条目算会让行内速度减半并在每行边界跳一格
    （票 #60 批次 9 r2）；列表档一行 = 一条、算式与旧口径一致。
  - 可见条目数按**各可见条目露出比例求和**，不用整数计数（票 #60 r6）。
  - **长度下限 64dp**——条目再多也不再变短；列表不足一屏、或不足两条时不显示。
- **拖动**：按住滑条拖动即连续快速定位、实时跟手，松手停在对应位置
  （网格档落点是该行的首条）。
  - 只认越过触摸斜率的手势（「压一下不带出定位」）；触摸与鼠标左键都能操作。
- **带内手势**归滑条：上下拖动 = 跳到该处（不是平滑滚动列表）；
  点击不传给下面的条目（可见期间这条抓取带不传点击，完全隐藏即整条移除）。
  - 但带内**鼠标滚轮照常滚动列表**（由滑条按内建换算代列表滚）。
- 与下拉更新及条目点击分层（按下滑条时事件不到达二者）。

_Avoid_: 滚动条、索引条

**导航过渡（Nav Transition）**：
返回与进入共用的过渡：**横向滑入 300ms**。

- 新屏 `translateX` 从 ±30% → 0（与旧屏**同幅**）。
- 旧屏**同向**移出 30% 并淡到 0.55；两屏同时动、不做错开。
- 两屏位移**同用旧屏那条加速曲线**；冷启动的纯淡入支仍用减速曲线。
- `ui/AppNav.kt` 的 `NavTransitions`，按方向预先建好各支 ⇒ 一次导航只触发一次过渡。
- 方向**由入口显式给出**（阅读器路由参数 `enter` + 纯函数 `navTransitionDirection`）：
  - 进入子文件夹 / 抽屉入口 / 进入阅读器 = 从右滑入
  - 返回上一级 / 抽屉返回 / 退出阅读器 = 从左滑入
  - 换书按入口（「下一本」与 `forward = true` 从右，「上一本」与 `forward = false` 从左）
  - 冷启动直接落进阅读器只淡入
- 阅读器等页期间新屏是主题背景色纯色、无加载指示，页就绪后整屏淡入 150ms。
- **点开书立刻滑入**（票 #122）：导航在点击/入口那一帧发生，**不再等前置**。
  - 「打开书 + 首批解码」的前置工作与它的等待改到**阅读页侧**
    （上限仍是 1.5s，到点由阅读页自己开书；发起侧那次等待与导航无关，
    只决定这次调用何时返回）。
  - 前置工作归会话级作用域（发起那一屏被导航销毁也带不走它）。
  - **四条开书入口都是「先导航再等」**（前屏不再保持到前置就绪，有意为之）。
- 前置按**哪一次打开**认主（世代号）：被顶替的世代不入槽，
  阅读页自己开书后迟到的也会被退役（界面重建不再取走它）。
- 四条入口共用**同一前置槽**；「这次点击算不算数」的守卫**各入口各一条**：
  - 浏览页 = 组合存活标志 + 「当前要开的那一本」
  - 另三条 = 「**栈项身份**（route + entryId）」
- 落地（进度覆盖 + 上次阅读位置）只发生在这个阅读页在屏幕上时——
  `#108`「取消不导航」在导航已发生之后的对应。

_Avoid_: 转场动画、翻页动画

**阅读菜单过渡（Reader Menu Transition）**：
阅读菜单面板的出现与消失（票 #129）。

- 呼出时从**屏幕下缘往上滑入**、收起时**沿来路往下滑回**。
- 时长 **250ms**（出现减速型、消失加速型）。
- 位移取整幅高 ⇒ 两端都完全落在屏幕下缘之外；面板的内容与几何不受影响。
- 声明口径在 `ui/ReaderMenuTransitions.kt`（`ReaderMenuTransitionsTest` 钉它）。

_Avoid_: 菜单动画、面板淡入

**条目名称断行（Entry Name Wrapping）**：
条目名称按「能塞就塞」断行（最多两行、不省略号）。

- 在 `]`、`-` 这类闭合/连接标点之后与数字字母交界处补零宽空格作为额外断点。
- 票 #92 起：「长度 ≥ 4 的 ASCII 字母/数字串」在串内每 2 个字符也补一个，
  因此可就地断开、把行尾填满，不引入连字符；3 字符以下与其它文字保持原子。
- 并给名称文本指定贪心断行配置，避免局部不可断单元把第一行提前截断而留下大段空白。

_Avoid_: 自动换行、文本截断

**名称自然排序（Logical Name Sort）**：
与 Windows 资源管理器一致的名称比较规则。

- 特殊字符先于数字，数字先于字母，假名先于汉字。
- 数字按数值比较（2 < 10）。
- 字母不区分大小写。
- 假名按五十音序（平假名与片假名同权）。
- 汉字按拼音。

_Avoid_: 字典序、字母排序

**修改时间排序（Modified Time Sort）**：
按文件的最后修改时间（mtime）排序；所有来源支持。
_Avoid_: 时间排序、更新时间排序

**发布时间排序（Release Time Sort）**：
按书的发布日期排序；所有来源支持；书缺少发布日期元数据时（如图片文件夹）
回退为修改时间排序。
_Avoid_: 发行时间排序
