# 构建产物
.gradle/
build/
app/build/

# 参考图（维护者本地素材，不入库）
references/

# 临时文件
tmp/

# pi agent 运行时状态（本地，不入库）
.pi/
.pi-web/

# implement-pro 编排产物（本地，不入库）
.implement-pro/

# 交接文档（本地，不入库；靠名字定位，不锚定路径以免以后搬到子目录就漏掉）
HANDOFF.md

# Kotlin 编译器会话目录（构建产物）
.kotlin/

# 项目 agent 规矩文档（本地保留，不入库；维护者 2026-09-22 决定）
AGENTS.md
