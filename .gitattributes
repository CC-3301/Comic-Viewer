# 行尾统一由 git 规范化：索引与工作区都按 LF 存储/检出。
#
# 2026-09-23：由 `* text=auto` 改成 `* text=auto eol=lf`，把工作区也钉到 LF
#（此前只规范了索引，工作区里的 CRLF 会在每次 `git add` 时触发「CRLF 将被 LF 替换」警告）。
#
# 例外：Windows 批处理必须是 CRLF —— cmd.exe 对 LF 结尾的 .bat 在 goto/标签处会出错，
# 而本仓的 Windows 门禁命令正是 `gradlew.bat testDebugUnitTest`。
# `eol=crlf` 只影响检出形式，索引里仍是 LF。
#
# 二进制文件不做行尾转换。

* text=auto eol=lf

*.bat text eol=crlf

*.jar binary
*.apk binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.zip binary
*.cbz binary
